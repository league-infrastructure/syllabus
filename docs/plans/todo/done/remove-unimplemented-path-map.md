---
status: pending
---

# Remove or Implement Unimplemented path_map() Method

`Course.path_map()` in `models.py` is a stub — the body is just `...`.
Dead code that suggests an incomplete feature. Either implement it if
it's needed or remove it to reduce confusion.
